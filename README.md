# expensesplit-pro
Full-stack expense splitting &amp; settlement optimization platform. .NET 8 Minimal API + React 18 + TypeScript + AWS Lambda + SQL Server. Features greedy algorithm (O(n log n)) to minimize settlement transactions.
